package rtm

/**
 * Controller related to {@link rtm.Record} model class.
 * We are using scaffolding here to generate views and 
 * controller actions at run time.
 */
class RecordController {
    static scaffold = rtm.Record // <-- magic starts here
    //def index() { }
}
